---
trigger: always_on
---

* Whenever code changes or code changes are accepted and merged, automatically bump the version number
* Follow semantic versioning (MAJOR.MINOR.PATCH)
* Update version in all relevant files (package.json, setup.py, version.txt, etc.)
* Increment PATCH for bug fixes, MINOR for new features, MAJOR for breaking changes
* Ensure version number is consistent across all files
